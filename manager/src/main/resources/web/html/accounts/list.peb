{% extends web_root + "fragments/template.peb" %}
    {% block title %}Accounts{% endblock %}
{% block content %}
    <div class="flex">
        <h1>Accounts</h1>
        <a class="button inline-button-right" href="/accounts/add">Add Account</a>
    </div>
    <hr>
    {% if error is not empty %}
        <div class="error">{{ error }}</div>
    {% endif %}
    <div id="app"></div>
{% endblock %}