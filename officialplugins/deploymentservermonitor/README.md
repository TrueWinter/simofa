# DeploymentServerMonitor Plugin

This plugin connects to your deployment servers every 5 minutes and sends a notification through Pushover if any of them are offline.

Requires the [Pushover Plugin](../pushover).